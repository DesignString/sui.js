/// <reference types="node" />
import tls = require('tls');
import http = require('http');
import { TlsRequest } from '../types';
import { DestroyableServer } from '../util/destroyable-server';
import { CAOptions } from '../util/tls';
export declare type ComboServerOptions = {
    debug: boolean;
    https: CAOptions | undefined;
    http2: true | false | 'fallback';
};
export declare function createComboServer(options: ComboServerOptions, requestListener: (req: http.IncomingMessage, res: http.ServerResponse) => void, tlsClientErrorListener: (socket: tls.TLSSocket, req: TlsRequest) => void): Promise<DestroyableServer>;
